<%= form_for(@kategory) do |f| %>
  <% if @kategory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@kategory.errors.count, "error") %> prohibited this kategory from being saved:</h2>

      <ul>
      <% @kategory.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
